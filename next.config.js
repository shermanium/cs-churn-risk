/**
 * Next.js configuration file
 *
 * See https://nextjs.org/docs/api-reference/next.config.js/introduction
 * for more details on configuring your Next.js project.
 *
 * This file replaces the previous `next.config.ts`, which is not
 * supported on the Vercel platform. Converting the TypeScript
 * configuration to a JavaScript module avoids build errors during
 * deployment.
 *
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  /**
   * Additional Next.js configuration options can be added here.
   * For example, to enable React Strict Mode:
   * reactStrictMode: true,
   */
};

module.exports = nextConfig;
